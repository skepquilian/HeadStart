<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.headstart">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SettingsActivity" />
        <activity android:name=".DriversActivity" />
        <activity android:name=".MapActivity" />
        <activity android:name=".TrucksActivity" />

        <activity android:name=".MainActivity">
            <!--
            intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter
            -->
        </activity>
        <activity
            android:name=".HomeActivity"
            android:parentActivityName=".TruckOwnerLoginForm">

            <!--
            The meta-data tag is required if you support API level 15 and lower >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".TruckOwnerLoginForm"  /
            -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TruckOwnerRegisterForm"
            android:parentActivityName=".TruckOwnerLoginForm" />
        <activity
            android:name=".TruckOwnerLoginForm"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".ResetPassword"
            android:parentActivityName=".TruckOwnerLoginForm" />
    </application>

</manifest>